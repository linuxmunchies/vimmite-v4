[Unit]
# An installation can remain offline for an arbitrary amount of time. Do not
# permanently rate-limit reconciliation before NetworkManager has connectivity.
StartLimitIntervalSec=0

[Service]
Restart=on-failure
RestartSec=2min
